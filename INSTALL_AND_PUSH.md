# 🚀 تثبيت Git ورفع المشروع - دليل كامل

## ⚠️ Git غير مثبت حالياً

---

## 📥 الخطوة 1: تثبيت Git

### 1. تحميل Git

**الرابط:** https://git-scm.com/downloads

- اختر **Windows**
- حمّل الملف (Git-2.xx.x-64-bit.exe)

### 2. تثبيت Git

1. شغّل الملف المحمّل
2. اضغط **Next** في كل خطوة (استخدم الإعدادات الافتراضية)
3. **أعد تشغيل PowerShell/CMD** بعد التثبيت

### 3. التحقق من التثبيت

افتح PowerShell/CMD جديد واكتب:

```bash
git --version
```

يجب أن ترى: `git version 2.xx.x`

---

## 🚀 الخطوة 2: رفع المشروع

### الطريقة 1: استخدام السكريبت (أسهل) ⭐

```bash
cd E:\whatsapp-bot
.\push-to-github.bat
```

### الطريقة 2: يدوياً

```bash
cd E:\whatsapp-bot

git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/alsydy/whatsapp-bot.git
git branch -M main
git push -u origin main
```

**ملاحظات:**
- سيطلب منك تسجيل الدخول إلى GitHub
- حجم الرفع: ~600 MB (10-15 دقيقة)

---

## 📱 الخطوة 3: تحميل APK

بعد الرفع:

1. اذهب إلى: **https://github.com/alsydy/whatsapp-bot**
2. اضغط **Actions**
3. اضغط **I understand my workflows, enable them** (إذا ظهرت)
4. انتظر 15-20 دقيقة
5. Actions > آخر workflow > **Artifacts** > **whatsapp-bot-apk** > Download

---

## ✅ بعد التحميل:

1. انسخ APK للهاتف
2. فعّل "مصادر غير معروفة"
3. ثبّت APK
4. افتح التطبيق!

---

**ابدأ بتثبيت Git من الرابط أعلاه!** 🚀

