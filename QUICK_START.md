# 🚀 دليل البدء السريع

## للتطوير على الكمبيوتر

### 1. التثبيت

```bash
npm install
```

### 2. التشغيل

```bash
# تشغيل الخادم فقط
npm start

# أو تشغيل مع إعادة تحميل تلقائي
npm run dev
```

### 3. الوصول

افتح المتصفح على: `http://localhost:3000`

---

## لبناء تطبيق أندرويد

### 1. التثبيت

```bash
npm install
```

### 2. بناء الواجهة

```bash
npm run build
```

### 3. إعداد Capacitor

```bash
npx cap sync android
```

### 4. فتح Android Studio

```bash
npx cap open android
```

### 5. في Android Studio

- انتظر حتى يكتمل التحميل
- اضغط `Shift+F10` أو زر Run
- اختر جهاز أو محاكي

---

## استخدام سريع

### إنشاء جلسة

1. اضغط "➕ جلسة جديدة"
2. أدخل اسم (اختياري)
3. اضغط "إنشاء"

### ربط واتساب

1. اضغط على الجلسة
2. امسح QR Code بواتساب
3. انتظر حتى يظهر "✅ متصل"

### إرسال رسالة

1. افتح الجلسة المتصلة
2. تبويب "💬 إرسال رسالة"
3. أدخل الرقم والرسالة
4. اضغط "📤 إرسال"

---

## ملاحظات مهمة

- ✅ الجلسات تُحفظ تلقائياً
- ✅ يعمل في الخلفية تلقائياً
- ✅ إعادة الاتصال التلقائي عند انقطاع الإنترنت
- ⚠️ يحتاج Node.js على الأندرويد (راجع SETUP_ANDROID.md)

---

## استكشاف الأخطاء السريع

### QR Code لا يظهر
→ تحقق من أن الخادم يعمل

### لا يمكن إرسال رسالة
→ تأكد من أن الجلسة متصلة

### التطبيق لا يعمل في الخلفية
→ فعّل "عدم تحسين البطارية" في إعدادات الأندرويد

