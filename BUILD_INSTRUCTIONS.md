# 🔨 تعليمات بناء تطبيق الأندرويد

## المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

1. **Node.js v24.12.0** أو أحدث
   - تحميل من: https://nodejs.org/
   - تأكد من إضافة Node.js إلى PATH

2. **Android Studio**
   - تحميل من: https://developer.android.com/studio
   - تثبيت Android SDK (API 21+)
   - تثبيت Java JDK 11 أو أحدث

## طريقة البناء السريعة (ويندوز)

### استخدام السكريبت الجاهز:

```bash
build.bat
```

هذا السكريبت سيقوم بـ:
1. تثبيت التبعيات
2. بناء الواجهة
3. مزامنة Capacitor
4. فتح Android Studio

## طريقة البناء اليدوية

### الخطوة 1: تثبيت التبعيات

```bash
npm install
```

### الخطوة 2: بناء الواجهة

```bash
npm run build
```

### الخطوة 3: مزامنة Capacitor

```bash
npx cap sync android
```

### الخطوة 4: فتح Android Studio

```bash
npx cap open android
```

## في Android Studio

### 1. انتظر التحميل
- انتظر حتى يكتمل تحميل المشروع (قد يستغرق بضع دقائق)
- تأكد من أن Gradle sync نجح

### 2. إعداد المحاكي أو الجهاز

#### للمحاكي:
- Tools > Device Manager
- Create Device
- اختر جهاز (مثل Pixel 5)
- اختر نظام (API 33 أو أحدث)
- اضغط Finish

#### للجهاز الحقيقي:
- فعّل "وضع المطور" على الهاتف
- فعّل "تصحيح USB"
- وصّل الهاتف بالكمبيوتر
- امنح الأذونات عند الطلب

### 3. بناء وتشغيل التطبيق

- اضغط `Shift+F10` أو زر **Run** (▶️)
- اختر الجهاز أو المحاكي
- انتظر حتى يكتمل البناء والتثبيت

## بناء APK للتثبيت

### Debug APK (للتجربة):

1. في Android Studio:
   - Build > Build Bundle(s) / APK(s) > Build APK(s)
   - انتظر حتى يكتمل البناء
   - APK سيكون في: `android/app/build/outputs/apk/debug/app-debug.apk`

2. انسخ APK للهاتف وثبته

### Release APK (للنشر):

#### 1. إنشاء Keystore:

```bash
keytool -genkey -v -keystore whatsapp-bot.keystore -alias whatsapp-bot -keyalg RSA -keysize 2048 -validity 10000
```

احفظ:
- كلمة مرور Keystore
- كلمة مرور Alias
- المعلومات الأخرى

#### 2. إعداد build.gradle:

افتح `android/app/build.gradle` وأضف:

```gradle
android {
    ...
    signingConfigs {
        release {
            storeFile file('whatsapp-bot.keystore')
            storePassword 'your-keystore-password'
            keyAlias 'whatsapp-bot'
            keyPassword 'your-key-password'
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}
```

#### 3. بناء Release APK:

- Build > Generate Signed Bundle / APK
- اختر APK
- اختر keystore
- أدخل كلمات المرور
- اختر release
- اضغط Finish

APK سيكون في: `android/app/release/app-release.apk`

## استكشاف الأخطاء

### المشكلة: npm غير معروف

**الحل:**
- تأكد من تثبيت Node.js
- أضف Node.js إلى PATH:
  - Windows: إعدادات > متغيرات البيئة > Path > إضافة مسار Node.js

### المشكلة: Gradle sync فشل

**الحل:**
- File > Invalidate Caches / Restart
- Tools > SDK Manager > تأكد من تثبيت Android SDK
- File > Sync Project with Gradle Files

### المشكلة: لا يمكن بناء التطبيق

**الحل:**
- تأكد من تثبيت Java JDK 11+
- تحقق من إعدادات Gradle
- امسح مجلد `.gradle` وأعد المحاولة

### المشكلة: التطبيق لا يعمل على الجهاز

**الحل:**
- تأكد من تفعيل "مصادر غير معروفة" في إعدادات الأندرويد
- تحقق من Logcat في Android Studio للأخطاء
- تأكد من أن الجهاز يدعم API 21+

## نصائح مهمة

1. **أول بناء قد يستغرق وقتاً طويلاً** (10-15 دقيقة)
2. **تأكد من وجود اتصال إنترنت** (لتحميل التبعيات)
3. **احفظ كلمات مرور Keystore** في مكان آمن
4. **اختبر التطبيق على جهاز حقيقي** قبل النشر

## الخطوات بعد البناء

1. ✅ ثبّت التطبيق على الهاتف
2. ✅ فعّل "عدم تحسين البطارية" للتطبيق
3. ✅ امنح جميع الأذونات المطلوبة
4. ✅ ابدأ استخدام التطبيق!

---

**ملاحظة:** إذا واجهت أي مشاكل، راجع ملف `SETUP_ANDROID.md` للحلول المتقدمة.

