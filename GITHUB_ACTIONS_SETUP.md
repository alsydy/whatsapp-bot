# 🚀 إعداد GitHub Actions لبناء APK

## ✅ ما تم إنجازه:

- ✅ ملف GitHub Actions جاهز: `.github/workflows/build-apk.yml`
- ✅ سيتم بناء APK تلقائياً عند كل push

---

## 📋 الخطوات:

### 1. إنشاء مستودع GitHub

1. اذهب إلى: https://github.com/new
2. أنشئ مستودع جديد باسم `whatsapp-bot`
3. **لا** تضع README أو .gitignore (سنضيفها لاحقاً)

### 2. رفع المشروع إلى GitHub

```bash
cd E:\whatsapp-bot

# تهيئة Git (إذا لم يكن موجوداً)
git init

# إضافة الملفات
git add .

# Commit
git commit -m "Initial commit - WhatsApp Bot Android App"

# إضافة remote
git remote add origin https://github.com/YOUR_USERNAME/whatsapp-bot.git

# رفع الملفات
git push -u origin main
```

**ملاحظة:** استبدل `YOUR_USERNAME` باسم مستخدمك في GitHub

### 3. تفعيل GitHub Actions

1. اذهب إلى مستودعك على GitHub
2. اضغط على تبويب **Actions**
3. إذا ظهرت رسالة "Workflows aren't being run on this repository"
4. اضغط **I understand my workflows, enable them**

### 4. بناء APK

**الطريقة 1: تلقائي**
- عند كل push، سيتم بناء APK تلقائياً
- اذهب إلى تبويب **Actions** لرؤية التقدم

**الطريقة 2: يدوي**
- اذهب إلى **Actions**
- اختر workflow **Build Android APK**
- اضغط **Run workflow**
- اضغط **Run workflow** مرة أخرى

### 5. تحميل APK

بعد اكتمال البناء:

1. اذهب إلى تبويب **Actions**
2. اضغط على آخر workflow run
3. في قسم **Artifacts**، اضغط على **whatsapp-bot-apk**
4. حمّل APK

**أو:**

- إذا تم إنشاء Release تلقائياً، ستجد APK في **Releases**

---

## ⚠️ ملاحظات مهمة:

### 1. حجم node_modules كبير

GitHub قد يرفض رفع `node_modules` (حجم كبير). 

**الحل:**
- أضف `node_modules/` إلى `.gitignore`
- GitHub Actions سيقوم بتثبيت التبعيات تلقائياً

### 2. ملفات Node.js binaries

تأكد من رفع ملفات `libnode.so` في `android/app/src/main/assets/nodejs/`

### 3. وقت البناء

- أول بناء: ~15-20 دقيقة
- البناءات التالية: ~10-15 دقيقة

---

## 🔧 إذا واجهت مشاكل:

### المشكلة: "Workflow not running"

**الحل:**
- تأكد من تفعيل Actions في إعدادات المستودع
- Settings > Actions > General > Allow all actions

### المشكلة: "Build failed"

**الحل:**
- تحقق من سجلات البناء في تبويب Actions
- تأكد من رفع جميع الملفات المطلوبة

### المشكلة: "APK not found"

**الحل:**
- تحقق من أن البناء اكتمل بنجاح
- ابحث في Artifacts

---

## ✅ بعد تحميل APK:

1. انسخ APK إلى هاتفك
2. فعّل "مصادر غير معروفة" في الإعدادات
3. ثبّت APK
4. افتح التطبيق وابدأ الاستخدام!

---

## 🎯 المميزات:

- ✅ **لا يحتاج تثبيت أي شيء على جهازك**
- ✅ **بناء تلقائي عند كل push**
- ✅ **APK جاهز للتحميل**
- ✅ **Release تلقائي** (اختياري)

---

**جاهز للرفع!** 🚀

